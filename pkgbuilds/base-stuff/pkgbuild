name="base-system"
version=1.0.0
depends=(glibc binutils xz lz4 zstd)
source="https://ftp.gnu.org/gnu/hello/hello-2.12.tar.gz"

# yes, this is a meta package.
# hello tar file is there because: 
# i didn't yet add logic to not fuck up no source installs 

build() {
    :
}

install() {
    :
}

